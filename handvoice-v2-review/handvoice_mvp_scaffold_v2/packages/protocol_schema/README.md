# Protocol schema

`handvoice_protocol.schema.json` defines the stable external shape of the versioned research protocol. The YAML file in `configs/` is the runtime configuration.

Protocol changes that alter task meaning, timing, prioritization instructions, or feature pairings require a new protocol version.
